const findById = (id, todoArray) => {

}
//
// const updateTodo = (id, todoArray) => {
//
// }
//
export const addNewTodo = (todo, todoArray) => {
  const todoList = todoArray.concat(todo);
  return todoList;
}
